<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Year_Range_Validation</fullName>
    <active>true</active>
    <description>Ensures the vehicle year is within the valid range of 1900 to the current year plus one.</description>
    <errorConditionFormula>NOT(ISBLANK(Year__c)) &amp;&amp;
            (Year__c &lt; 1900 || Year__c &gt; (YEAR(TODAY()) + 1))</errorConditionFormula>
    <errorDisplayField>Year__c</errorDisplayField>
    <errorMessage>Vehicle Year must be between 1900 and next calendar year.</errorMessage>
</ValidationRule>
